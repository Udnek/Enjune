namespace Enjune.Graphic.Key;

public enum KeyCode
{
    
    // keyboard
    Escape, 
    F1, F2, F3, F4, F5, F6, F7, F8, F9, F10, F11, F12,
    Q, W, E, R, T, Y, U, I, O, P,      Backspace,
     A, S, D, F, G, H, J, K, L,        Enter,
      Z, X, C, V, B, N, M,
    LeftShift, Space, RightShift,
    // mouse
    RightMouseButton,
    LeftMouseButton,
    
    
    // // letters
    // Q, W, E, R, T, Y, U, I, O, P,
    //  A, S, D, F, G, H, J, K, L,
    //   Z, X, C, V, B, N, M, 
    // // functional
    // F1, F2, F3, F4, F5, F6, F7, F8, F9, F10, F11, F12,
    // // special
    // LeftShift, RightShift, Escape, Backspace, Space, Enter,
    // // mouse
    // RightMouseButton,
    // LeftMouseButton,
}